# URL du backend Hono (utilisé par le proxy /api dans vite.config.ts).
# En local : http://localhost:3456 (défaut si la variable est absente)
# En prod : https://<railway-backend>.up.railway.app
VITE_BACKEND_URL=http://localhost:3456
