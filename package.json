{
  "name": "poplist",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n \"hono,next\" -c \"red,green\" \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2"
  }
}
