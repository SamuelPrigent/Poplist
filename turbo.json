{
  "$schema": "./node_modules/turbo/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "outputs": ["dist/**", ".vercel/output/**", ".nitro/**", ".output/**"],
      "env": ["VITE_BACKEND_URL", "VERCEL", "VERCEL_ENV"]
    },
    "lint": {
      "cache": true
    }
  }
}
